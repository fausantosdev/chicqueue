import { styled } from 'styled-components'

export const Container = styled.button`
    width: 100%;
    border: none;
    padding: .3rem;
    border-radius: 8px;
    color: var(--white);
    background: var(--secondary);

    span {
        font-size: 1rem;
    }
`